# Configuración de endpoints para Rasa

# Action server endpoint
action_endpoint:
  url: "http://localhost:5055/webhook"

# Tracker store (opcional - por defecto usa InMemoryTrackerStore)
# tracker_store:
#   type: sql
#   dialect: "sqlite"
#   db: "tracker.db"

# Event broker (opcional - para eventos asíncronos)
# event_broker:
#   type: pika
#   url: localhost
#   username: guest
#   password: guest

# Model server (opcional - para cargar modelos desde servidor remoto)
# models:
#   url: http://my-server.com/models/default_core@latest
#   wait_time_between_pulls: 10
